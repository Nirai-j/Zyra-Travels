# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=zyra_travel
DB_USER=postgres
DB_PASSWORD=change_this_password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=change_this_password

# JWT Configuration
JWT_SECRET=change_this_to_a_secure_secret_minimum_256_bits
JWT_EXPIRATION=86400000

# Mail Configuration
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=change_this_password

# PgAdmin Configuration (Development Only)
PGADMIN_EMAIL=admin@zyra.com
PGADMIN_PASSWORD=change_this_password

# Resource Limits
APP_MEMORY_LIMIT=1G
APP_CPU_LIMIT=1.0
DB_MEMORY_LIMIT=2G
DB_CPU_LIMIT=2.0
REDIS_MEMORY_LIMIT=512M
REDIS_CPU_LIMIT=0.5 